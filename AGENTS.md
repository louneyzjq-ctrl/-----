# AI海龟汤游戏开发指令（AGENTS）

## 1. 角色与目标
你是本项目的 AI 开发协作者。目标是在保证代码可维护与用户体验的前提下，快速交付 `PRD.md` 定义的 MVP：
- 游戏大厅
- 游戏对话页
- 汤底结果页
- AI 回答严格为“是/否/无关”

## 2. 项目上下文
- 产品文档：`PRD.md`
- 技术文档：`TECH_DESIGN.md`
- 任何实现、重构、修复都必须与以上两份文档一致

## 3. 输出原则
- 优先给出 **可运行代码**，避免只给概念性建议
- 变更应最小化，避免无关重构
- 先保证正确，再优化样式
- 遇到不确定需求，先采用保守实现并在注释中标记 TODO

## 4. 技术规范
- 前端：React + TypeScript + Vite + Tailwind CSS
- 后端：Node.js + Express（推荐，用于保护 API Key）
- API 通讯使用 `fetch`（或项目已有 HTTP 工具，保持一致）
- 不引入重量级依赖，除非有明确收益

## 5. 代码风格
- TypeScript 开启严格类型思维，避免 `any`
- 组件使用函数式组件 + Hooks
- 命名规范：
  - 组件：PascalCase
  - 变量/函数：camelCase
  - 常量：UPPER_SNAKE_CASE
  - 类型：`T` 前缀（如 `TStory`, `TMessage`）
- 单文件职责清晰，避免超大组件
- 复杂逻辑前可加 1-2 行简洁注释

## 6. UI/交互约束
- 视觉风格：神秘悬疑、深蓝色调
- 响应式优先，保证移动端可用
- 聊天区必须：
  - 用户消息右侧，AI 消息左侧
  - 新消息自动滚动到底部
  - 发送后输入框清空
- 必须包含加载态与错误提示

## 7. AI 回答硬性规则（最高优先级）
- AI 最终输出必须且只能是：`是`、`否`、`无关`
- 禁止输出解释、额外文本、标点修饰
- 若模型返回不合规，必须回退为 `无关`
- 不允许泄露汤底，不允许暗示答案

## 8. 安全规则
- 严禁硬编码 API Key
- 严禁把 `.env` 实际密钥内容写入代码或提交
- 前端环境变量仅用于开发便利，不视为安全方案
- 生产环境优先走后端代理

## 9. 实施流程（每次任务都遵循）
1. 先读取并理解相关文档和现有代码
2. 拆分为最小可交付步骤
3. 实现后先做本地自检（构建/运行/关键流程）
4. 修复明显问题后再交付
5. 输出变更说明：改了什么、为什么、如何验证

## 10. 完成定义（Definition of Done）
- 功能符合 `PRD.md` 的 MVP 验收项
- 代码能通过基本运行验证（至少可启动并完成核心流程）
- 类型错误、明显运行错误已处理
- 异常分支可用（网络失败、AI 不合规输出）
- 不引入新的安全风险

## 11. 常用任务指令模板
### 11.1 初始化项目
“请根据 `PRD.md` 与 `TECH_DESIGN.md` 初始化 React + TypeScript + Vite 项目，并配置 Tailwind 与路由，确保可运行。”

### 11.2 新增页面/组件
“请按文档要求创建 `Home/Game/Result` 页面与相关组件，保持类型完整、样式统一、移动端可用。”

### 11.3 接入 AI
“请实现 `askAI(question, story)`，严格执行三值回答约束；若输出不合规，统一回退为 `无关` 并返回可读错误信息。”

### 11.4 联调修复
“请根据报错日志定位问题，先给最小修复，再说明根因与验证步骤；不要做无关重构。”

## 12. 禁止事项
- 不得忽略或绕过三值回答约束
- 不得输出与任务无关的大规模重构
- 不得在未知前提下擅自改动项目架构
- 不得修改文档目标使其迎合实现难度
