# AI海龟汤游戏 PRD

## 1. 产品概述
AI海龟汤游戏是一个基于对话的推理小游戏网站。玩家选择一个“海龟汤”故事（汤面 + 汤底），阅读汤面后通过连续提问进行推理；AI 扮演主持人，只能根据汤底回答三种固定选项：**“是 / 否 / 无关”**。玩家可随时查看汤底揭晓真相并结束本局。

## 2. 目标
- **在浏览器中 0 门槛开玩**：无需登录即可开始游戏。
- **核心体验稳定**：AI 回答严格受控（只输出“是/否/无关”），避免泄底和长篇解释。
- **可快速扩展内容**：新增故事（数据）不需要改动核心流程。

## 3. 目标用户
- 喜欢推理、解谜、脑洞类小游戏的用户
- 想快速体验 AI 交互应用的泛用户
- AI 应用学习者（希望通过项目练习前后端与部署）

## 4. 使用场景
- 通勤碎片时间娱乐
- 朋友聚会时一起玩（一个人操作、多人讨论）
- 学习/展示 AI 产品 Demo

## 5. 产品范围
### 5.1 MVP（必须做）
1. **游戏大厅**
   - 展示故事列表（标题、难度、简短介绍/标签）
   - 选择故事进入游戏页
2. **游戏页（对话推理）**
   - 显示汤面内容
   - 聊天输入：提问、发送、回车发送
   - 消息列表：区分用户/AI 气泡样式，自动滚动到底部
   - AI 只回答“是/否/无关”
   - “查看汤底”“结束游戏”按钮
3. **汤底揭晓页（结果页）**
   - 显示汤底真相（重点突出）
   - 可选：展示本局对话历史
   - “再来一局”（回大厅）
4. **基础体验与容错**
   - AI 请求中的加载态（如“思考中...”）
   - 失败时友好提示与重试引导
   - 移动端适配

### 5.2 后续版本（可选）
- 用户登录与历史记录保存
- 排行榜/通关次数/最少问题数
- 用户自定义海龟汤（投稿与审核）
- 多人房间/对战/计时模式
- 更丰富的答案体系（例如“可能是”）与更严格判定机制（需要更复杂规则/模型）

## 6. 关键体验原则（产品约束）
- **回答严格约束**：AI 输出必须严格为“是”“否”“无关”之一；任何不合规输出都要被拦截/回退处理。
- **不泄底**：AI 不得解释原因，不得主动补充信息，不得暗示汤底。
- **节奏轻快**：发问-回应必须反馈迅速（无论成功/失败都要有清晰状态）。
- **可玩性靠内容**：故事质量、难度分级、仪式感揭晓是留存关键。

## 7. 信息架构与页面
### 7.1 页面列表
- `/`：游戏大厅（Home）
- `/game/:id`：游戏页（Game）
- `/result/:id`：结果页（Result）

### 7.2 页面要素与交互
#### A. 游戏大厅（Home）
- 标题：AI海龟汤
- 简介：玩法说明（1-2 行即可）
- 故事卡片（列表/网格）
  - 信息：标题、难度（easy/medium/hard）、可选一句话简介
  - 交互：hover/点击进入 `/game/:id`

#### B. 游戏页（Game）
- 顶部：故事标题 + 汤面（可折叠/滚动区域）
- 中部：聊天区域
  - 消息：用户在右、AI 在左；有头像/图标
  - 自动滚动：新消息出现滚动到底部
- 底部：输入框 + 发送按钮
  - 回车发送（Shift+Enter 换行可选）
  - 发送后清空输入
- 操作区按钮：
  - “查看汤底” → `/result/:id`
  - “结束游戏” → 返回 `/`

#### C. 结果页（Result）
- 标题：故事标题
- 汤底：重点展示（分段/强调）
- 可选：对话历史回顾
- “再来一局”按钮：返回大厅
- 视觉：揭晓仪式感（简单动画/渐入/光效即可）

## 8. 核心用户流程（MVP）
1. 进入大厅 `/`
2. 选择故事 → 进入 `/game/:id`
3. 阅读汤面 → 连续提问
4. AI 返回“是/否/无关” → 形成推理循环
5. 玩家选择“查看汤底”揭晓 → `/result/:id`
6. 点击“再来一局”回大厅

## 9. 功能需求详述（MVP）
### 9.1 故事数据
- 字段（最小集）：
  - `id: string`
  - `title: string`
  - `difficulty: 'easy' | 'medium' | 'hard'`
  - `surface: string`（汤面）
  - `bottom: string`（汤底）
- 要求：
  - 默认内置 3-5 个故事
  - 可按难度过滤/排序（可选）

### 9.2 对话与状态管理
- 消息结构（最小集）：
  - `id: string`
  - `role: 'user' | 'assistant'`
  - `content: string`
  - `timestamp: number`
- 状态：
  - `messages[]`
  - `inputText`
  - `isLoading`
  - `errorMessage?`
  - `currentStory`

### 9.3 AI 回答规则（强约束）
#### 输入输出
- **输入**：
  - `question`：用户本轮提问（自然语言）
  - `story.surface`：汤面
  - `story.bottom`：汤底（仅作为 AI 判断依据，不展示给玩家）
- **输出**：必须严格为下列之一（不带标点、不带解释、不带引号）：
  - `是`
  - `否`
  - `无关`

#### 行为约束
- AI **不得**输出任何解释、建议、推理过程或额外信息
- AI **不得**主动给提示，不得引导玩家提问方向
- AI **不得**透露汤底、不得暗示“接近了/差一点”等
- 当玩家输入不是问题（如陈述、辱骂、无意义文本）时，仍必须在三选一中给出最合适的回答（通常为“无关”）

#### 不合规回退策略（前端/后端均可实现）
- 若 AI 返回内容不在 {是, 否, 无关} 内：
  - **优先**：进行一次自动纠正请求（可选，带更严格约束）
  - **否则**：直接回退为 `无关`，并在 UI 给出提示：`AI回答不规范，请换个问法再试。`

### 9.4 AI 请求状态与错误处理
- 加载态：发送后立即追加一条临时 AI 消息（如 `思考中...`），返回后替换为最终答案
- 错误态：
  - 网络/超时：提示 `网络不稳定，请稍后重试`
  - 服务端错误：提示 `AI回复出错了，稍后再试`
  - 允许用户继续提问（不阻断会话）

### 9.5 导航与会话规则
- 进入 `/game/:id` 时初始化本局对话（可选：给出开场白，但必须符合“是/否/无关”约束；因此 MVP 建议不主动发言）
- 点击“结束游戏”：清空本局状态并返回大厅
- 点击“查看汤底”：跳转结果页，展示汤底（可选展示对话历史）

## 10. 非功能需求
### 10.1 兼容性
- 支持主流现代浏览器（Chrome/Edge/Safari 最新两个大版本）
- 移动端适配：宽度 360px 起可用，聊天输入与按钮不遮挡

### 10.2 性能与体验
- 首屏可交互时间：在普通网络环境下尽量 < 3 秒（以 Vite 构建的静态站点为目标）
- 聊天列表渲染：消息较多时仍可滚动流畅（MVP 可先不做虚拟列表）

### 10.3 安全与隐私（约束）
- **严禁**将 AI API Key 硬编码进仓库
- 若采用前端直连模型：必须使用环境变量注入，并在文档中提示“前端环境变量不等于安全，生产建议走后端代理”
- 若采用后端代理：后端从环境变量读取 Key，并仅暴露 `/api/chat` 等必要接口

## 11. 视觉与交互风格要求
- **整体风格**：神秘悬疑、深蓝色调
- **推荐配色**：
  - 背景：深蓝/深灰（如 slate/indigo 系）
  - 强调色：金色/琥珀色（用于标题、按钮高亮）
- **布局**：
  - 大厅：卡片网格，响应式 1-3 列
  - 游戏页：上（汤面）中（聊天）下（输入/操作）三段式
- **动效（轻量）**：
  - 卡片 hover 浮起/发光
  - 结果页汤底渐入（营造揭晓仪式感）

## 12. 验收标准（MVP）
- **大厅**：能展示 ≥3 个故事；点击任一故事可进入对应游戏页
- **游戏页**：
  - 汤面正确显示
  - 能发送消息；回车发送可用（或明确仅按钮发送）
  - 新消息追加后自动滚动到底部
  - AI 返回内容始终为 `是/否/无关` 之一（不合规有回退与提示）
  - “查看汤底”“结束游戏”按钮可用且符合导航规则
- **结果页**：能展示汤底；“再来一局”可回大厅
- **基础体验**：有加载态与错误提示；移动端可正常使用
